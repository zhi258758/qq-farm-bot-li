/**
 * 已废弃：QQ 扫码登录已完全迁移到参照仓库方案。
 *
 * 新实现见 `core/src/services/napcat-login.ts`，对外接口为 `/api/napcat-login/*`，
 * 配置通过环境变量提供（NAPCAT_LOGIN_ENABLED / NAPCAT_WEBUI_URL / NAPCAT_OPENAUTH_URL / NAPCAT_TOKEN）。
 * 本文件仅保留占位，可安全删除。
 */
export {};
